 
##final GitHub repo submission rubric

(Detective Layer Implementation
Evaluate the forensic evidence collection capability of the detective nodes. This assesses whether the detectives use structural analysis (AST parsing, PDF chunking, image extraction) to produce typed Evidence objects, rather than relying on brittle text matching. This covers the analysis logic and the quality of evidence produced.
High
20 pts •
RepoInvestigator uses AST parsing to verify not just class existence but structural patterns (e.g., add_edge() call patterns, fan-out wiring, reducer usage). Git log extraction captures commit messages and timestamps and can identify progression patterns vs. bulk uploads. DocAnalyst implements chunked PDF ingestion (RAG-lite) enabling targeted queries rather than full-document context dumping. VisionInspector equivalent is implemented with image extraction from PDFs and multimodal LLM analysis (execution of the vision analysis is optional, but the implementation must exist). All detectives output fully populated Evidence objects with goal, found status, location, rationale, and confidence.
Above Average
12 pts •
Average
4 pts •
Non-existent
0 pts •
Graph Orchestration Architecture
Evaluate the LangGraph StateGraph wiring for parallel execution patterns. This assesses whether detectives and judges are orchestrated with fan-out/fan-in parallelism and whether error-handling edges exist. This criterion covers the graph topology, not the node implementations themselves.
High
25 pts •
Two distinct parallel fan-out/fan-in patterns: one for the detective layer and one for the judicial layer. An evidence aggregation node sits between the two layers, collecting all detective outputs before dispatching to judges. Conditional edges handle error states (e.g., failed clone, missing evidence, malformed judge output). The full flow is: START -> [Detectives in parallel] -> Aggregation -> [Judges in parallel] -> Synthesis -> END. The graph compiles, executes end-to-end, and produces a final report from a repo URL input.
Above Average
15 pts •
Average
5 pts •
Non-existent
0 pts •
Judicial Persona Differentiation & Structured Output
Evaluate whether the judicial layer implements three genuinely distinct personas (Prosecutor, Defense, Tech Lead equivalents) with conflicting evaluation philosophies, and whether their outputs are enforced as structured, validated objects. This criterion assesses prompt distinctness and output enforcement, not the graph wiring that runs the judges.
High
20 pts •
Three clearly distinct personas with genuinely conflicting system prompts. The Prosecutor prompt explicitly instructs adversarial scrutiny (looking for gaps, security flaws, laziness). The Defense prompt explicitly instructs recognition of effort, intent, and creative workarounds. The Tech Lead prompt explicitly focuses on architectural soundness, maintainability, and practical viability. All judges use .with_structured_output() or .bind_tools() bound to the JudicialOpinion schema. Retry or error-handling logic exists for malformed outputs. Judges receive rubric criteria dynamically and evaluate each criterion through their specific lens.
Above Average
12 pts •
Average
4 pts •
Non-existent
0 pts •
Chief Justice Synthesis Engine
Evaluate whether the final synthesis node resolves conflicts between judge opinions using deterministic, hardcoded Python logic rather than delegating entirely to an LLM prompt. This also assesses whether the output is a structured Markdown report. This criterion covers the synthesis logic and report serialization, not the judge implementations that feed it.
High
20 pts •
Deterministic Python if/else logic implements multiple named conflict resolution rules: security override (security flaws cap scores), fact supremacy (detective evidence overrules unsupported judge claims), and functionality weight (Tech Lead opinion carries highest weight for architecture). Score variance > 2 triggers a dissent summary or re-evaluation step. The output is a complete Markdown report with Executive Summary, per-criterion breakdown (including all three judge opinions and dissent where applicable), and a Remediation Plan with specific, file-level instructions. The report is written to a file, not just printed to console.
Above Average
12 pts •
Average
4 pts •
Non-existent
0 pts •
Generated Audit Report Artifacts
Evaluate whether the repository contains generated audit reports (self-audit and peer-audit) that follow the AuditReport model structure. This assesses the output products, not the code that generates them.
High
15 pts •
Three report types are present: self-audit, peer-audit (generated by running the agent against a peer's repo), and peer-received (the report a peer's agent generated against this repo). Reports follow the AuditReport structure: Executive Summary with overall score, per-criterion breakdown (covering all rubric dimensions) with final score, individual judge opinions with cited evidence, dissent summaries where applicable, and a Remediation Plan with specific file-level instructions.
Above Average
9 pts •
Average
3 pts •
Non-existent
0 pts •)




##langsmith trace link rubric:

--End-to-End Trace Completeness
Determines whether the trace captures a full pipeline execution from initial input (repo URL and PDF path) through to a final rendered audit report, with no terminal failures that prevent the graph from reaching its end node. Evaluated based on: 1. Trace Accessibility: The shared LangSmith link loads and displays a valid trace 2. Node Coverage: All expected layers (detectives, evidence aggregation, judges, chief justice synthesis) appear in the trace tree 3. Completion: The pipeline reaches its terminal node and produces structured output 4. Error Recovery: Any mid-pipeline errors are handled via conditional edges or retries rather than causing silent failures or crashes
Mastered
20 pts •
The trace shows a clean, unbroken execution from START to END. Every expected node (detectives, aggregator, judges, chief justice) appears in the trace tree and completes successfully. If any node encountered an error, the trace shows a conditional edge or retry path that recovered gracefully before continuing. The final node produces a complete structured output.



## final report submission

--Executive Summary
Executive Summary is evaluated based on: 1. Scope Communication: Does the summary convey the system's purpose and the architectural approach taken? 2. Outcome Reporting: Does it state the overall self-audit result with an aggregate score? 3. Key Takeaways: Does it surface the most important findings from the peer feedback loop and remaining gaps? 4. Actionability: Could a senior engineer reading only this section understand the project's status and next steps?
High
10 pts •
The summary gives a complete snapshot: the architectural approach, the self-audit verdict with an aggregate score, the most impactful finding from the peer feedback loop, the top remaining gap, and the primary remediation priority. A senior engineer reading only this section could decide whether to read further or act on the findings immediately.
Above Average
6 pts •
Average
2 pts •
Non-existent
0 pts •
Architecture Deep Dive and Diagrams
Architecture Deep Dive is evaluated based on: 1. Conceptual Grounding: Does the report explain Dialectical Synthesis, Fan-In/Fan-Out, and Metacognition with concrete connections to architectural elements, not just as buzzwords? 2. Data Flow Clarity: Does the report describe how evidence flows from Detectives through Judges to the Chief Justice synthesis? 3. Diagram Quality: Does at least one diagram accurately depict the StateGraph with visually distinct parallel branches and synchronization points? 4. Design Rationale: Are trade-offs discussed with reasoning (e.g., why Pydantic over dicts, why deterministic rules over LLM averaging)?
High
30 pts •
All three concepts (Dialectical Synthesis, Fan-In/Fan-Out, Metacognition) are explained with concrete architectural grounding: Dialectical Synthesis is tied to three conflicting judge personas and deterministic resolution logic; Fan-In/Fan-Out is tied to specific graph edges showing parallel Detective and Judge execution with synchronization nodes; Metacognition is connected to the system's ability to evaluate evaluation quality. At least one diagram accurately depicts the StateGraph with visually distinct parallel branches for both Detectives and Judges, fan-in synchronization points, and the synthesis endpoint. Design trade-offs (e.g., why Pydantic over dicts, why deterministic synthesis rules over LLM-based averaging) are discussed with rationale.
Above Average
18 pts •
Average
6 pts •
Non-existent
0 pts •
Self-Audit Criterion Breakdown
Self-Audit Criterion Breakdown is evaluated based on: 1. Structure: Are results organized per rubric dimension with individual scores for each? 2. Evidence Traceability: Can the reader trace from detective evidence to judge opinions to final verdict? 3. Dialectical Tension: Does the breakdown show how the three judge personas (Prosecutor, Defense, Tech Lead) assessed each dimension, including disagreements? 4. Honesty: Are weak dimensions addressed directly rather than hidden or inflated?
High
25 pts •
Every rubric dimension has a dedicated breakdown showing the final score, a summary of how the three judge personas assessed that dimension (including disagreements), and an honest explanation of why the final score landed where it did. Weak dimensions are addressed directly, not hidden. The reader can trace from evidence to judge opinions to final verdict for each dimension.
Above Average
15 pts •
Average
5 pts •
Non-existent
0 pts •
MinMax Feedback Loop Reflection
MinMax Feedback Loop Reflection is evaluated based on: 1. Peer Findings Received: Does the report identify specific issues the peer's agent found in the trainee's work? 2. Response Actions: Does the report describe concrete changes made to code or agent behavior in response to peer feedback? 3. Peer Audit Findings: Does the report describe what the trainee's agent discovered when auditing the peer? 4. Bidirectional Learning: Does the reflection show how being audited improved the trainee's own auditor, revealing a systemic insight beyond individual fixes?
High
20 pts •
The report presents a complete feedback cycle: specific peer findings received, specific code or agent changes made in response, specific findings from auditing the peer, and -- critically -- how the experience of being audited led the trainee to improve their own auditor's detection capabilities. The reflection identifies a pattern or insight that goes beyond individual bug fixes (e.g., 'Our Prosecutor persona was too lenient on sandboxing checks because we ourselves had not prioritized it until the peer agent flagged it').
Above Average
12 pts •
Average
4 pts •
Non-existent
0 pts •
Remediation Plan
Remediation Plan is evaluated based on: 1. Specificity: Does each item identify the gap, the affected rubric dimension, and the file or component to modify? 2. Actionability: Could an engineer unfamiliar with the codebase pick up this plan and begin executing it? 3. Prioritization: Are items ordered by impact or dependency rather than listed arbitrarily? 4. Completeness: Does each item describe the concrete change to make and why it would improve the score?
High
15 pts •
The plan is structured as a prioritized action list. Each item identifies the gap, references the rubric dimension it affects, specifies the file or component to modify, describes the concrete change to make, and explains why that change would improve the score. Items are ordered by impact or dependency. An engineer unfamiliar with the codebase could read this plan and begin executing it.
Above Average
9 pts •
Average
3 pts •
Non-existent
0 pts •


## demo video submission:
---End-to-End Pipeline Execution
Verify that the presenter runs the full auditor swarm live, from accepting inputs (a GitHub repo URL and a PDF report, which may reside within the repo itself) through to a rendered Markdown audit report as final output. This criterion assesses whether the system actually executes, not whether the architecture is elegant.
Mastered
5 pts •
The presenter invokes the swarm with a real repo URL and PDF (which can exist within the repo itself) as inputs. The pipeline runs to completion on screen. A final Markdown audit report is shown as the output artifact, containing scores, reasoning, and remediation. The execution is clearly not mocked or pre-recorded output pasted into a terminal.
Competent
3 pts •
Developing
1 pts •
Unsatisfactory
0 pts •
Layered Architecture Visibility
Verify that the video makes the hierarchical, multi-agent structure observable. The grader should be able to see distinct outputs from the Detective layer (structured evidence), the Judicial layer (conflicting opinions from distinct personas), and the Synthesis layer (a final verdict that references the conflict). This criterion assesses the visibility of the architecture in the demo, not the code quality.
Mastered
5 pts •
All three layers are distinctly visible during execution. Detective agents produce structured Evidence objects (not free text). At least two judge personas (e.g., Prosecutor, Defense) produce visibly conflicting scores or arguments for the same evidence. The Chief Justice output references the disagreement and shows a resolved final score. The viewer can trace data flowing from one layer to the next.
Competent
3 pts •
Developing
1 pts •
Unsatisfactory
0 pts •
Concept Translation & Strategic Justification
Assess whether the presenter can articulate the 'why' behind their architectural decisions. The grader evaluates whether the presenter translates complex concepts (Dialectical Synthesis, Fan-In/Fan-Out orchestration, deterministic conflict resolution) into clear explanations and connects them to the business objective of autonomous governance at scale.
Mastered
5 pts •
The presenter clearly explains the strategic purpose of at least two major architectural choices (e.g., why parallel fan-out for detectives, why three judge personas instead of one grader, why deterministic rules in the Chief Justice instead of another LLM call). Complex terms are translated into plain reasoning. The presenter connects their architecture to the broader problem of automated quality assurance at scale.
Competent
3 pts •
Developing
1 pts •
Unsatisfactory
0 pts •
Professional Delivery & Pacing
Assess the production quality, time management, and structural clarity of the video as a professional technical demonstration. The grader evaluates whether the presenter respects the 5-minute time constraint and delivers a focused, logically ordered walkthrough without wasted time.
Mastered
5 pts •
The video is within the 5-minute limit. It follows a clear logical structure (e.g., context/setup, live execution, layer-by-layer walkthrough, final output). Pacing is confident with no significant filler, rambling, or dead time. Audio and video quality are sufficient to follow all on-screen content.
Competent
3 pts •
Developing
1 pts •
Unsatisfactory
0 pts •




# for the peer grading i must do this are what we are supposed to follow:
Development Progress
Evaluate how much of the complete Week 2 deliverable the peer has implemented, as evidenced by the repository's git history and the functional state of the codebase. This is measured against the full final spec: state definitions, detective tools, judge nodes, Chief Justice synthesis, complete graph wiring, infrastructure, and audit reports. This criterion focuses on delivery completeness and the quality of the engineering process visible in git history, not report quality or theoretical depth.
Complete System
35 pts •
Atomic, meaningful commits telling a clear development narrative across all phases. The full pipeline executes end-to-end: repo URL input through detectives (parallel fan-out/fan-in), through judges (parallel with distinct personas and structured output), through Chief Justice (deterministic conflict resolution), to a rendered Markdown audit report. All required files are present and substantive: state definitions with reducers, sandboxed tools, detective nodes, judge nodes with .with_structured_output(), justice node with hardcoded rules, complete graph with conditional edges, infrastructure files, and generated audit reports in audit/. The system has been run against at least the peer's own repo, producing a real report. Nothing structurally missing from the final deliverable spec. Any minor remaining gaps are documented with a clear rationale.
Partial Pipeline
21 pts •
Superficial
7 pts •
Absent
0 pts •
Feedback Implementation
Evaluate whether the peer acted on feedback received during the development period -- from your direct communication, your code review, or from audit reports generated by your agent. This measures the peer's ability to iterate based on external input, which is the core of the MinMax loop. This criterion does not assess whether the feedback itself was correct. It assesses whether the peer engaged with received feedback and made visible, traceable changes as a result. If no feedback was exchanged, score 0 (No Exchange) and exclude from the weighted total.
Fully Integrated
20 pts •
The peer addressed all substantive feedback points with traceable commits. Where feedback could not be fully addressed, the peer explicitly documented the deferral with a rationale (in commit messages, communication, or the report). Agent-generated audit findings were used to drive concrete architectural improvements, not just cosmetic fixes. The peer's response demonstrates comprehension of the feedback, not mechanical application. Evidence of the MinMax loop: feedback received led to improvements in both the peer's code and their understanding of what their own agent should detect. Nothing left unaddressed without documented reasoning.
Selective
12 pts •
Ignored
4 pts •
No Exchange
0 pts •
Proactive Communication
Evaluate whether the peer proactively communicated about their progress, blockers, design decisions, and coordination needs throughout the development period. This measures initiative in making the peer-grading collaboration functional. This criterion does not assess technical skill or communication style. It assesses whether the peer made the feedback loop possible by being reachable, forthcoming, and engaged.
Collaborative Driver
20 pts •
The peer regularly shared progress, flagged blockers early, and actively sought your input on design decisions and implementation trade-offs. They shared their repo and intermediate work early and often, enabling multiple rounds of feedback. They engaged with your agent's output and discussed findings. Communication was distributed across the development period. The peer treated the collaboration as a continuous loop, not a one-time handoff. The collaboration was genuinely bidirectional and productive throughout.
Engaged
12 pts •
Reactive Only
4 pts •
Absent
0 pts •
Agent Feedback Relevance
Evaluate whether YOUR auditor agent produced relevant, actionable, forensic-quality feedback when run against the peer's repository. This is a dual assessment: it tests whether the peer's repo is structured enough to be machine-auditable AND whether your agent is capable of generating genuine signal. If your agent crashes or produces no output, score 0 or 1 regardless of the peer's repo state. If the peer's repo is empty, your agent should handle that gracefully and report the absence -- that itself is valid forensic output. Be honest: if your agent failed to produce useful output, score accordingly. Inflating your own agent's performance undermines the adversarial improvement process.
Full Forensic Audit
25 pts •
Your agent produced a complete audit report following the Automaton Auditor spec. Detectives collected structured evidence: git history analysis with progression assessment, AST-based code structure verification (not just regex), file existence with content analysis, and cross-referencing between report claims and repo reality. Judges (or equivalent analysis) provided multi-perspective evaluation with distinct viewpoints. The output includes a synthesized verdict with criterion-level scores, cited evidence, dissent where judges disagreed, and a specific remediation plan with file-level instructions. The feedback is detailed enough for the peer to take direct action. The agent demonstrates the full detective-judicial-synthesis pipeline against real input, producing output that matches the AuditReport structure.
Targeted Analysis
15 pts •
Generic Noise
5 pts •
No Output
0 pts •

